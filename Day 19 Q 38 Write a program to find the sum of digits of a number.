#include <stdio.h>

int main() {
     int num;
     int sum=0;
     printf("enter a number\n");
     scanf("%d", &num);
    while(num>0)
    {
        int d=num%10;
        sum=sum+d;
        num=num/10;
    }
   printf("the sum of digits is%d\n", sum);
    return 0;
}


