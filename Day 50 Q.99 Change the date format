
#include <stdio.h>

int main() {
    int d, m, y;

    printf("Enter date (DD/MM/YYYY): ");
    scanf("%d/%d/%d", &d, &m, &y);

    printf("Converted format (YYYY-MM-DD): %04d-%02d-%02d\n", y, m, d);

    return 0;
}
